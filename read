if (null == pickingHead.getCode()) {
            pickingHead.setCode(genPickCode(pickingHead.getBizLineCode()));
           lineList.forEach(item -> item.getPickLine().setStorageOutPickingCode(pickingHead.getCode()));
        }
